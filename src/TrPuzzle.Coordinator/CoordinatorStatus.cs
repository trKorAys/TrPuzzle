namespace TrPuzzle.Coordinator;

/// <summary>The distributed coordinator is intentionally disabled for the first release.</summary>
public static class CoordinatorStatus
{
    public static bool IsEnabled => false;
}
